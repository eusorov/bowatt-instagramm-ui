services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8080
    container_name: bowatt-instagramm-ui
    ports:
      - "8081:80"
    restart: unless-stopped